
// querySelectorAll devuelve todos los elementos q encuentra
const card = document.querySelectorAll( '.card' );
console.log(card);

const cardDos = document.querySelectorAll( 'section.hospedaje .card' );
console.log(cardDos);